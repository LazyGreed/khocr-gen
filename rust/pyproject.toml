[build-system]
requires = ["maturin>=1.0"]
build-backend = "maturin"

[project]
name = "khocr-gen-core"
version = "0.1.0"
requires-python = ">=3.12"

[tool.maturin]
module-name = "_rust_accel"
features = ["pyo3/extension-module"]
bindings = "pyo3"
